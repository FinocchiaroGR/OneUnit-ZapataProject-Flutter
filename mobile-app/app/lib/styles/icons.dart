import 'package:flutter/material.dart';

const arrowLeft = Icon(Icons.arrow_back_ios_new_sharp);
const arrowRight = Icon(Icons.arrow_forward_ios_sharp);
const documents = Icon(Icons.assignment_sharp);
const contact = Icon(Icons.whatsapp_sharp);
const service = Icon(Icons.construction_sharp);
const sellCar = Icon(Icons.car_rental_sharp);
const home = Icon(Icons.home_sharp);
const user = Icon(Icons.person_sharp);
const help = Icon(Icons.help_sharp);

/*
const person = Icon(Icons.person);
const menu = Icon(Icons.menu);
const info = Icon(Icons.info);

const folder = Icon(Icons.folder);
const location = Icon(Icons.near_me);
const paid = Icon(Icons.paid);
const eventAvaible = Icon(Icons.event_available);
const touchApp = Icon(Icons.touch_app);

const localParking = Icon(Icons.local_parking);
const geoFence = Icon(Icons.share_location);
const speed = Icon(Icons.speed); //Speed y Info para velocidad maxima

const login = Icon(Icons.login);
const logout = Icon(Icons.logout);
const close = Icon(Icons.close);

// Quizas
const directionsCarFilled = Icon(Icons.directions_car_filled);
const viewList = Icon(Icons.view_list);
*/
